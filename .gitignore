# Secrets — never commit. .env.* covers .env.production (loaded by main.go via
# godotenv), which the old "*.env" pattern missed.
.env
.env.*
*.env
backend/.env
backend/.env.*
!.env.example
!backend/.env.example

# Uploaded payment proofs — parent PII, never commit
uploads/

# SQLite database files
*.db
*.db-shm
*.db-wal

# Go build output
backend/studyhub

# Python
__pycache__/
*.pyc
services/analytics/.venv/
services/analytics/venv/

# Docker
.docker/

# OS
.DS_Store
Thumbs.db


notes.txt